# ChatGPT Reasoning Effort Toggle

- Userscript for `https://chatgpt.com/*`
- Adds `min/max` buttons near the composer
- Hotkey: `Cmd+O` toggles between `min` and `max` (and blocks the site's default `Cmd+O`)

> Note: Title shows “（只适用于Pro）” per the original requirement.
